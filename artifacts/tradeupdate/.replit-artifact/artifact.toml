kind = "web"
previewPath = "/"
title = "TradeUpdate"
version = "1.0.0"
id = "artifacts/tradeupdate"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5000

[services.development]
run = "pnpm --filter @workspace/tradeupdate run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/tradeupdate", "run", "build" ]
publicDir = "artifacts/tradeupdate/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5000"
BASE_PATH = "/"
